{
  "name": "portfolio-website",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sass": "sass --watch scss/:dist/css/ --no-source-map",
    "sass:build": "sass scss/:dist/css/ --no-source-map",
    "dev": "npm run sass:build && vite",
    "build": "npm run sass:build && vite build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.83.0",
    "vite": "^7.3.1"
  }
}
